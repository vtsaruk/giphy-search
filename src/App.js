import { Container } from '@mui/material/'
import { NavPanel, GifsList } from './components'


function Search () {
  return (
    <Container>
      <NavPanel />
      <GifsList />
    </Container>
  )
}

export default Search